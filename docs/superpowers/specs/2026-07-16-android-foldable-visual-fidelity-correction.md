# Android 折叠屏视觉还原纠偏

状态：用户已以 `codex-clipboard-923d146a-2ca5-46e2-be5b-4911bb2406ae.png` 确认此稿为唯一实现基准，立即执行。

## 纠偏结论

上一轮只完成了颜色替换和基础自适应，未还原确认稿的模块比例与信息密度。后续验收以该图的三个独立内屏页面及窄长外屏页面为准，不以“使用相近颜色”视为通过。

## 固定视觉合同

- 外屏 1140×2616：卡片紧凑、首屏信息密度高、自然纵向滚动；不得出现当前设置页那种一个模块占满半屏的巨型卡片。
- 内屏 2248×2480：首页必须为左导航 + 主队列工作区 + 右侧添加任务/默认质量/任务进度辅助工作区；历史必须为两列完成时间线；设置必须是双列卡片网格，账号与权限不可独占全宽。
- 首页：顶部“运行状态”是紧凑统计行；下载队列使用可扫描的紧凑行、缩略图、状态与规格；添加任务及智能读取使用右栏橙色主操作。
- 历史：顶部搜索和筛选紧凑；完成记录按日期轨道分布到双列卡片，三点菜单只保留在卡片右侧。
- 设置：YouTube 首位，抖音和 TikTok 同组；账号、默认分辨率、任务行为、文件存储以同尺寸或相近尺寸模块平铺；平台标识必须具有平台辨识度，不能以相同绿色通用图标代替。
- 字号、留白和圆角按确认稿收紧：外屏页面标题不超过 24sp、模块标题不超过 18sp、正文 13–15sp；常规卡片内部留白 12–16dp；不得为无数据状态制造大面积空白。
- 薄荷绿是背景与结构基调，紫色/土色/暖橙仅对应明确功能模块；选中态保持实心绿色容器。

## 验收

使用同一状态的外屏与内屏独立截图逐页比对：模块位置、栏数、相对面积、文本密度和操作位置均需与确认稿一致。任何空状态也必须保留该页面的工作区骨架，不得退化成放大的单列占位卡。
