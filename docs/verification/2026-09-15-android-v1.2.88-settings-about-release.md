# Android v1.2.88 设置“关于”与正式交付验证

日期：2026-09-15（Asia/Shanghai）

## 本次范围

- 设置页新增“关于”卡片，显示 APK 版本、构建时间、开发者信息和项目 GitHub 标识。
- `BuildConfig` 在正式构建时写入上海时区构建时间；版本升至 `1.2.88 / 10288`。
- 新增 `SettingsAboutContractTest`，锁定关于卡片、构建元数据、项目仓库标识、功能审阅条目和展开布局入口。

## 自动与产物证据

| 项目 | 已确认事实 |
| --- | --- |
| JVM／lint | `SettingsAboutContractTest`、设置等高合同测试的 Debug／Release JVM 回归，以及 Debug／Release lint 通过。 |
| 正式构建 | `:app:stageFormalReleaseArtifacts` 成功；Release 签名、R8 和 `verifyReleaseParcelableCreatorRetention` 门禁通过。构建命令离线复用既有缓存并临时关闭 Gradle 依赖校验；未修改仓库校验元数据。 |
| APK | `android/app/build/outputs/formal-release/南枫下载-Android-v1.2.88.apk`，46,054,992 B，SHA-256 `5c7d09a33976dd09962d362ffaf14648be8e9c4a1c85effb766d2adca6cb5f88`。 |
| 身份 | 包名 `com.nanzhufeng.videodownloader`，`1.2.88 / 10288`；v2 签名已验证，证书 SHA-256 `c4fb47e276b5a9381e5362e8d176ccb9e171a034f513c9d811d47a53640f4547`。 |

## 设备与界面边界

- 用户授权后，OPPO 仅执行同签名的 `pm install -r --user 0` 覆盖。安装后设备回拉 `base.apk` 的 SHA-256 与上述本地 APK 相同，证书相同；首次安装时间保持 `2026-08-21 02:04:16`，CE／DE 数据 inode 保持 `1788131`／`1745695`。未清数据、未启动 OPPO App、未运行 `connected*AndroidTest`。
- 新增预览 `docs/screenshots/github-preview/settings-about-v1.2.88.png` 来自隔离 Android 15 模拟器，展开视口 `1600×1800`、字体比例 `1.0`。界面实际展示 `Android 版 1.2.88（10288）`、构建时间及 GitHub 项目标识；这只证明模拟器渲染，不代替 OPPO 的人工视觉验收。

## 文档冲突与遗留项

- `PROJECT_HANDOFF.md` 在本次验证前已有未提交修改，且其中最近版本仍为 `1.2.87`。为避免覆盖用户工作区，未直接改写；本文件、`android/app/build.gradle.kts` 和已回拉验证的 APK 是 1.2.88 的当前证据。
- 视频详情面板、播放器顶部安全区和双击播放／暂停仍待 OPPO 内外屏人工验收。GitHub Release 附件须在远端上传后重新读取并下载校验，才算下载交付完成。
