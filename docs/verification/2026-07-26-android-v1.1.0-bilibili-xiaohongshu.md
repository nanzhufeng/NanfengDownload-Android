# 南枫下载 Android v1.1.0 哔哩哔哩与小红书扩展验证

更新时间：2026-07-26（Asia/Shanghai）

## 产品范围

- 新增哔哩哔哩与小红书/Rednote 单视频链接识别、短链解析、去重、下载列表、传输、MediaStore 发布、历史记录、平台图标和永久吞吐报告。
- 哔哩哔哩公开视频支持独立视频流与音频流下载、合并；使用平台要求的页面来源和媒体请求头。
- 小红书只接受视频笔记，图文笔记返回中文问题与解决办法；公开单视频无需登录。
- 哔哩哔哩 UP 主页批量接口当前在真实网络中返回 412，`v1.1.0` 不声明支持该能力。主页链接会直接提示打开具体视频并重新分享。
- 不绕过会员、付费、DRM、私密或平台访问控制。

## 自动化门禁

| 层级 | 结果 |
|---|---|
| Python 解析测试 | 17 项，0 失败 |
| Debug JVM | 140 项，0 失败、0 错误、0 跳过 |
| Release JVM | 140 项，0 失败、0 错误、0 跳过 |
| Release Lint | 通过 |
| 专用模拟器仪器测试 | 58 项，0 失败；外部 TikTok 条件测试按既有规则跳过 |
| ABI | `arm64-v8a`、`x86_64` |

自动测试只安装到 `emulator-5554 / NanzhufengFindN5Api35`，没有把测试 APK 安装到 OPPO。

## 模拟器真实网络闭环

两条样本均在 `1140×2616 / 442dpi` 专用模拟器中完成：

| 平台 | 样本 | 结果 |
|---|---|---|
| 哔哩哔哩 | 公公开视频 `BV1bK411W797` | 读取、入队、下载、音视频合并、MediaStore、历史完成；720p、约 7.5 MB、单连接，平均约 2.1 MB/s、峰值约 8.2 MB/s |
| 小红书/Rednote | 公公开视频笔记 `69ce30d3000000002100791c` | 读取、入队、下载、MediaStore、历史完成；720p、约 10.7 MB、多连接 ×3，平均约 2.1 MB/s、峰值约 7.8 MB/s |

同一小红书链接再次智能读取时，界面明确提示“该作品已存在于下载列表或历史中，未重复添加”。

以上数字证明当前样本的真实链路，不承诺所有 CDN、网络或内容都达到固定速度。

## 会话与界面

- 设置页按长视频/短视频分组展示五个平台，哔哩哔哩与小红书使用独立图标。
- 哔哩哔哩官方 H5 手机号登录页可正常打开；登录 Cookie 仍需在实际受限内容使用时再次验证。
- 小红书消费者网站没有稳定的独立移动网页登录入口；失效的 `/login` 页面已移除。当前支持范围为公开单视频免登录，设置页使用不可点击的“无需登录”，不保留假登录按钮。
- 默认界面只显示中文问题与可执行解决办法；原始底层错误继续保留在高级详情。

## 正式验收产物

| 项目 | 结果 |
|---|---|
| 版本 | `1.1.0` / `versionCode 10100` |
| SDK | `minSdk 29` / `targetSdk 35` |
| APK | `android/app/build/outputs/formal-release/南枫下载-Android-v1.1.0.apk` |
| APK 大小 | 91,327,503 B |
| APK SHA-256 | `8f03cb499aa0db94394bcd8e23070cab163a80520343a1dbfa8062331b37f47a` |
| AAB | `android/app/build/outputs/formal-release/南枫下载-Android-v1.1.0.aab` |
| AAB 大小 | 38,262,440 B |
| AAB SHA-256 | `fafcd2f2f50cabed3241fecc5256e98434269c0b3c583884087fb6b7158aaea7` |
| 证书 SHA-256 | `c4fb47e276b5a9381e5362e8d176ccb9e171a034f513c9d811d47a53640f4547` |

APK 已验证为 `debuggable=false`、双 ABI、签名与压缩结构正常；专用模拟器冷启动成功，从模拟器拉回的 `base.apk` 与本地 APK 哈希一致。

## OPPO 正式版真实闭环

设备：OPPO Find N5 / `PKH120`。

- 使用固定安全路径“推送 APK → `pm install -r --user 0`”，从 `v1.0.0 / 10000` 同签名无损覆盖到 `v1.1.0 / 10100`。
- `firstInstallTime` 保持 `2026-07-18 13:44:10`；旧历史与输入草稿保留，没有卸载或清除数据。
- 从 OPPO 拉回的 `base.apk` 为 91,327,503 B，SHA-256 与本地产物同为 `8f03cb499aa0db94394bcd8e23070cab163a80520343a1dbfa8062331b37f47a`。

| 平台 | 样本 | OPPO 结果 |
|---|---|---|
| 哔哩哔哩 | 公公开视频 `BV1bK411W797` | 读取、入队、下载、音视频合并、MediaStore、历史完成；选择“720p 及以下”预设，实际成品 852×480、7.5 MB、单连接，平均 3.8 MB/s、峰值 19.4 MB/s |
| 小红书/Rednote | 公公开视频笔记 `69ce30d3000000002100791c` | 读取、入队、下载、MediaStore、历史完成；实际成品 1280×720、10.7 MB、多连接 ×3，平均 4.3 MB/s、峰值 21.8 MB/s |

MediaStore 读回：

- 哔哩哔哩成品 7,880,911 B，位于 `Movies/南烛枫视频下载器/哔哩哔哩/打牌还是打桩/`。
- 小红书成品 11,217,043 B，位于 `Movies/南烛枫视频下载器/小红书/国际素材研究中心/`。
- 从 OPPO MediaStore 读回后用 `ffprobe` 验证：哔哩哔哩成品含 HEVC 852×480 视频轨和 AAC 双声道音轨；小红书成品含 H.264 1280×720 视频轨和 AAC 双声道音轨。
- 再次读取同一小红书链接时，界面明确提示“该作品已存在于下载列表或历史中，未重复添加”。
- 强制停止并重启 App 后，两条历史、平台、大小、连接模式与速度报告仍可读取。

## 尚未完成的发布门槛

- GitHub 最新公开正式版仍为 `v1.0.0`；本轮没有上传或创建 `v1.1.0` Release。
- GitHub 发布前应复核源码 checkpoint、Release 附件名、SHA-256 文件和远端下载回读，不得只推送源码。
