# 南枫下载 Android v1.0.0 正式发布验证

日期：2026-07-21（Asia/Shanghai）

## 发布身份

- applicationId：`com.nanzhufeng.videodownloader`
- versionName：`1.0.0`
- versionCode：`10000`
- minSdk / targetSdk：`29 / 35`
- APK：不可调试，使用 APK Signature Scheme v2
- 正式证书 SHA-256：`C4:FB:47:E2:76:B5:A9:38:1E:53:62:E8:D1:76:CC:B9:E1:71:A0:34:F5:13:C9:D8:11:D4:7A:53:64:0F:45:47`

签名密钥和密码不进入项目、GitHub、日志或本文。密钥保存在仓库外，密码由 macOS 钥匙串管理。

## 自动门禁

执行：

```text
:app:testDebugUnitTest
:app:testReleaseUnitTest
:app:lintRelease
:app:stageFormalReleaseArtifacts
--max-workers=1 --rerun-tasks
```

结果：

- Debug JVM：127 项，0 失败、0 错误、0 跳过。
- Release JVM：127 项，0 失败、0 错误、0 跳过。
- Release Lint：通过。
- 117 个 Gradle 任务全部实际执行，`BUILD SUCCESSFUL in 2m 31s`。
- arm64-v8a 与 x86_64 均包含 `libmp3lame.so` 和 `libnanzhufeng_mp3.so`。

## 冻结产物

| 产物 | 大小 | SHA-256 |
|---|---:|---|
| `南枫下载-Android-v1.0.0.apk` | 91,291,547 B | `460a310aa8bfa50ae5cbc4683682c858b0dbc040659bc9300925874a8b88cbca` |
| `南枫下载-Android-v1.0.0.aab` | 38,245,189 B | `70f62007511626186330856765edebb30d269a1d5b0e1578234ad90613b4bab3` |

APK 与 AAB 均通过 ZIP 完整性检查；APK 通过 `apksigner verify --verbose --print-certs`；AAB 通过 `jarsigner -verify` 和 Google bundletool 1.16.0 `validate`。

## 运行验证

- 专用模拟器：`emulator-5580`，1140×2616 / 442dpi。
- 移除模拟器旧 Debug 包后安装正式 APK，冷启动进入当前首页。
- 从模拟器拉回已安装 `base.apk`，SHA-256 与本地正式 APK 完全一致。
- OPPO Find N5 未安装本次正式版：设备上旧 Debug 包签名不同，覆盖会失败；本轮未卸载、未清数据。

## 仍待验证

- 本轮没有用正式签名版重新执行 YouTube、抖音、TikTok 真实下载；业务代码未改变，既有 OPPO 三平台证据仍保留，但不冒充本轮新证据。
- 正式版切换到 OPPO 前必须明确旧 Debug App 内数据的迁移或放弃方案。
- 正式签名密钥需要用户控制的异地加密备份；本机安全存储不是完整灾难恢复方案。
