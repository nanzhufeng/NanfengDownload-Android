# 南枫下载 Android v1.2.5 历史批量选择布局验收

日期：2026-07-27（Asia/Shanghai）

## 目标与边界

- “批量删除”与“全部平台 / 全部时间”固定在同一行，减少常态界面占用。
- 卡片内部不再显示复选框；选择功能合并到时间线左侧圆形标记。
- 左侧选择控件必须在卡片外独立呈现，并与对应视频卡片垂直居中。
- 沿用 `v1.2.4` 的批量删除数据语义，不重新实现或扩大删除范围。
- OPPO 保存真实用户历史，真机只验证进入、选择和退出，不确认删除。

## 实现事实

- `HistoryFilters` 统一拥有平台、时间和批量删除三个同排入口。
- 历史卡片本体不再为选择框预留横向空间。
- 选择态下，时间线左侧使用独立可点击圆形控件：未选为中性描边圆圈，选中为绿色 `CheckCircle`。
- 普通完成标记与选择控件共用同一条时间线轴心；任务行从顶部对齐改为垂直居中。
- 卡片主体在选择态仍可切换选择，保留较大的可点击区域和无障碍描述。

## 自动验证

- Debug JVM：164 项，0 失败、0 错误、0 跳过。
- Release JVM：164 项，0 失败、0 错误、0 跳过。
- Release Lint：通过。
- 专用模拟器 `emulator-5580`：77 项仪器测试，0 失败；3 项外部 TikTok 条件测试按缺少实时参数跳过。
- 历史页定向界面测试：9/9 通过。
- 新增几何断言：左侧选择控件必须完全位于卡片左侧，二者垂直中心差小于 4dp。
- `v1.2.4` 的 Room 批量删除、去重、全选当前筛选结果和二次确认测试继续全量通过。

## 视觉与 OPPO 真机验证

- 1140×2616 外屏模拟器：常态筛选与“批量删除”同排；选择态左侧圆圈清晰独立，卡片内不再重复显示选择框。
- 选中第一条后，左侧圆圈原位变为绿色勾号，卡片文本宽度和行高不发生跳动。
- OPPO PKH120 的 UI 层级实测：
  - “全部平台”文本边界 `[88,518][228,560]`。
  - “全部时间”文本边界 `[338,518][478,560]`。
  - “批量删除”文本边界 `[923,514][1063,564]`。
  - 三项中心纵坐标基本一致，确认处于同一行。
- OPPO 进入选择态后，第一条左侧选择控件边界为 `[72,987][149,1064]`；点击后界面显示“已选 1 项”，无障碍描述变为“取消选择 …”。
- 真机未确认删除任何记录；退出选择态后不执行破坏性操作。
- 从 OPPO 拉回的 `base.apk` 与本地正式 APK SHA-256 完全一致。

## 正式产物

- APK：`android/app/build/outputs/formal-release/南枫下载-Android-v1.2.5.apk`
  - 91,539,663 B
  - SHA-256：`b2da131229d82d2cf39751a309241f13e0a97ee4902a077000a497f22a568e61`
- AAB：`android/app/build/outputs/formal-release/南枫下载-Android-v1.2.5.aab`
  - 38,337,478 B
  - SHA-256：`7b444e71226ba14a8fbbe2847628fe8550aa72f365d3bb1329e7723ff54dd93a`
- APK：`1.2.5 / 10205`、`minSdk 29`、`targetSdk 35`、arm64-v8a + x86_64、不可调试。
- 正式证书 SHA-256：`c4fb47e276b5a9381e5362e8d176ccb9e171a034f513c9d811d47a53640f4547`。
- OPPO `firstInstallTime` 保持 `2026-07-18 13:44:10`。

## 未重复执行

- 本轮没有重新执行五个平台真实网络下载；下载、分段、播放和历史媒体信息沿用既有已完成证据。
- 没有重新验证实际删除用户历史；破坏性链路继续由隔离模拟器和 Room 测试承担。
- 没有上传 GitHub 或创建 Release。
