# Android v1.2.86 设置卡片对齐、清除按钮与输入计数验证

日期：2026-09-01（Asia/Shanghai）

## 本次范围

- 展开设置页前两行使用成对行容器。账号与权限／下载质量、下载规则／保存位置每对均以较高内容卡的高度为行高，左右卡片填满该行；较短卡片只留下底部空白，不移除或截断内容、交互或设置项。
- 账号与权限中所有“清除”从 `OutlinedButton` 改为浅灰实底 `Button`；绿色主操作保留。
- 添加任务字符数移出输入框右侧，放到输入框下方的单独右对齐行。展开右栏和紧凑底部入口共同复用 `ReadEntryCard`，所以内外屏均使用此布局。

## 自动验证

- `SettingsExpandedCardAlignmentContractTest`：锁定两组成对行、`IntrinsicSize.Min` 行高和左右 `fillMaxHeight()`；同时锁定“清除”不再使用描边并具有浅灰底。
- `HomeLayoutResourceTest`：锁定字符数不再是 `trailingIcon`，输入框下方单行右对齐，不占链接文字宽度。
- 上述测试连同侧栏、白卡契约的 Debug/Release JVM 回归通过；Debug/Release lint 通过。
- `stageFormalReleaseArtifacts` 通过：Release 签名配置门禁、R8 `verifyReleaseParcelableCreatorRetention` 门禁、正式 APK/AAB 均生成成功。
- Gradle 常规依赖校验仍因本地 `junit-bom-5.9.3.module` 缓存和 `verification-metadata.xml` 哈希不匹配受阻。本轮只离线使用既有缓存，并临时使用 `--dependency-verification=off`；未修改校验元数据。

## 正式产物与 OPPO 覆盖

- APK：`android/app/build/outputs/formal-release/南枫下载-Android-v1.2.86.apk`
- 大小：46,038,888 B
- 包名／版本：`com.nanzhufeng.videodownloader`，`1.2.86 / 10286`
- APK SHA-256：`fc0da075cee1b25437ebfcd9af75f851dcc8037a07c3bede3230883241ba0e83`
- APK v2 签名已验证；证书 SHA-256：`c4fb47e276b5a9381e5362e8d176ccb9e171a034f513c9d811d47a53640f4547`

安装前 OPPO 为 `1.2.85 / 10285`，无 `DEBUGGABLE` 标记，首次安装时间为 `2026-08-21 02:04:16`，CE/DE 数据 inode 为 `1788131`／`1745695`。用户授权后以 `pm install -r --user 0` 覆盖。

安装后设备为 `1.2.86 / 10286`；首次安装时间、CE/DE 数据 inode 未变化。回拉设备实际 `base.apk` 的 SHA-256 为 `fc0da075cee1b25437ebfcd9af75f851dcc8037a07c3bede3230883241ba0e83`，与本地正式 APK 完全一致，签名证书也一致。未启动 App、未操作用户下载或历史记录、未运行 `connected*AndroidTest`；设备及本地临时 APK 已清理。

## 尚未验证

- 未进行 v1.2.86 内外屏人工视觉验收；构建、安装和契约测试不能证明实际卡片高度或输入文本可读性。
- 未重新执行用户手动冷启动复验。
- 本次覆盖安装不构成下载、MediaStore 或外部平台真实链路验收，也未发布 GitHub Release。
