# Android v1.2.84 工作台表面与 OPPO 覆盖安装验证

日期：2026-09-01（Asia/Shanghai）

> 更正（同日）：用户提供的 OPPO 截图确认 v1.2.84 的侧栏实现错误。`PrimaryNavigationRail` 内的 `Box.fillMaxSize()` 在 `NavigationRail` 未固定横向宽度时撑满屏幕，导致右侧页面不可见、入口组相对整屏居中。该文档原先关于“侧栏居中”的实现结论已撤销；v1.2.85 以 `NavigationRail.width(80.dp)` 限定侧栏自身范围并已覆盖安装。白卡、输入框和清空按钮的材质调整不受此错误影响。

## 本次范围

- 展开侧栏将“主页／历史／设置”入口组置于导航栏纵向中心，顶部应用图标和紧凑底部导航不变。
- 复用 `WorkbenchCard` 的最外层白卡移除硬边描边，继续保留圆角和 1dp 浅阴影。
- 添加任务的链接输入框及“清空”按钮改为无描边浅灰实底；不改复制、字符数、键盘避让、智能读取或清空行为。

## 自动验证

- Debug/Release JVM：`NavigationRailLayoutContractTest`、`WorkbenchPaletteContractTest`、`HomeLayoutResourceTest` 通过。
- Debug/Release lint 通过。
- `stageFormalReleaseArtifacts` 通过：Release 签名配置门禁、R8 `verifyReleaseParcelableCreatorRetention` 门禁、正式 APK/AAB 均生成成功。
- Gradle 的常规依赖校验未能通过：本地 `junit-bom-5.9.3.module` 缓存与 `verification-metadata.xml` 的哈希不匹配。本次命令只离线使用已存在的缓存，并临时使用 `--dependency-verification=off`；未修改校验元数据。这限制的是依赖完整性门禁，不影响下面对已生成 APK 的版本、签名和设备回读事实。

## 正式产物

- APK：`android/app/build/outputs/formal-release/南枫下载-Android-v1.2.84.apk`
- 大小：46,038,888 B
- 包名／版本：`com.nanzhufeng.videodownloader`，`1.2.84 / 10284`
- APK SHA-256：`c4b6250ce6b9e53e61e1342372fc6bd8800039d18202ac560e4ffdd41a03456b`
- APK v2 签名已验证；证书 SHA-256：`c4fb47e276b5a9381e5362e8d176ccb9e171a034f513c9d811d47a53640f4547`

## OPPO 保数据覆盖

安装前已只读核对：现装版本为 `1.2.83 / 10283`、无 `DEBUGGABLE` 标记、首次安装时间为 `2026-08-21 02:04:16`、CE/DE 数据 inode 为 `1788131`／`1745695`，证书与新 APK 相同。

用户授权后使用 `pm install -r --user 0` 覆盖。安装后设备为 `1.2.84 / 10284`，首次安装时间和 CE/DE 数据 inode 均未变化。回拉设备实际 `base.apk` 的 SHA-256 为 `c4b6250ce6b9e53e61e1342372fc6bd8800039d18202ac560e4ffdd41a03456b`，与本地正式 APK 完全一致，签名证书也一致。

未启动 App、未操作用户下载或历史记录、未运行 `connected*AndroidTest`；设备及本地临时 APK 已清理。

## 尚未验证

- 尚未在 OPPO 对侧栏、白卡和添加任务控件进行人工视觉验收。
- 尚未重新执行用户手动冷启动复验。
- 本次覆盖安装不构成下载、MediaStore 或外部平台真实链路验收，也未发布 GitHub Release。
