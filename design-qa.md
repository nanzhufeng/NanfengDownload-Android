# 南烛枫视频下载器：六屏 UI 设计验收

日期：2026-07-17

## 验收范围

- 外屏：首页、历史、设置，统一根视口 `1140 × 2616`，单页显示，底部导航。
- 内屏：首页、历史、设置，统一根视口 `2248 × 2480`，单页显示，左侧导航。
- 参考源：用户提供的外屏与内屏预览板；预览板仅用于视觉、层级和模块关系，不作为“多页同屏”实现依据。
- 实现状态：真实 Compose 页面、真实筛选与导航状态、模拟器专用样本数据。

## 对照证据

六组参考与实现并排图：

- `.artifacts/ui-contract-2026-07-17/final-pairs/outer-home.png`
- `.artifacts/ui-contract-2026-07-17/final-pairs/outer-history.png`
- `.artifacts/ui-contract-2026-07-17/final-pairs/outer-settings.png`
- `.artifacts/ui-contract-2026-07-17/final-pairs/inner-home.png`
- `.artifacts/ui-contract-2026-07-17/final-pairs/inner-history.png`
- `.artifacts/ui-contract-2026-07-17/final-pairs/inner-settings.png`

## 对照结果

### 布局与响应式

- 外屏严格为单列页面与底部导航；首页首屏展示运行状态、两条队列和完整添加任务模块。
- 内屏严格为左侧导航与统一内容视口；首页为主队列加右侧任务栏，历史为双列时间线，设置为 2 × 2 语义卡片。
- 三页在同一设备形态下共享一致的导航、安全区、页面边距和根高度；超长内容由页面内部滚动承担。
- 未发现内容重叠、横向裁切、导航错位或把三页同时塞入一个 App 画面的情况。

### 字体、间距与表面

- 标题、模块标题、正文、说明和标签形成稳定五级层次；长标题使用受控省略或换行。
- 外屏运行状态、队列与历史卡片经过第二轮密度压缩，首屏层级与参考板一致。
- 页面底色为 `#E6EAE7` 低饱和灰绿；所有内容卡片统一为纯白 `#FFFFFF`，不再使用紫、绿、土色容器底色。
- 卡片使用 `tonalElevation = 0dp` 阻断 Material3 自动绿色叠加，保留 1dp 独立阴影与中性边框；外屏中心像素实测为 `#FFFFFF`。

### 颜色、图标与状态

- 主色为淡绿色；橙色仅用于等待和智能读取，紫色用于下载质量，土色用于文件保存。
- 选中的底部导航、左侧导航、筛选和分辨率均有完整模块底色，不只改变文字颜色。
- YouTube、抖音和 TikTok 使用真实品牌图形资源；未使用文字方块、Emoji 或临时占位图。
- App 图标继续使用已确认的 `16dp` 留白版本，本轮未再次放大、裁切或更换适配策略。

### 交互与可访问性

- 首页、历史、设置采用无淡入、无滑动的即时切换；导航选中色只保留 120ms 局部反馈，不延迟页面内容。
- 平台与时间筛选、分辨率单选、开关、登录/导入按钮和历史三点菜单均为真实控件。
- 设置页的“同时下载”、“文件命名”、“下载路径”和“导出 cookies.txt”已由静态文字改为真实操作入口，分别接通并发队列、命名策略、SAF 文件夹授权和 Netscape Cookie 导出。
- 关键图标具有语义描述，主要状态有文字和颜色双重表达。
- 模拟器全套 44 项仪器测试完成，0 失败；3 项仅因外部 TikTok 实时条件跳过。外屏和内屏均未出现交互目标被遮挡或不可见。

## 验证记录

- `:app:testDebugUnitTest`：通过。
- `:app:lintDebug`：通过。
- `:app:assembleDebug`：通过。
- 模拟器全套仪器测试：`44 executed / 0 failures / 3 external-condition skipped`。
- 新增白卡实测证据：`.artifacts/ui-contract-2026-07-17/pure-white-settings/outer-settings.png`、`inner-settings.png`。
- 同视口参考/实现并排图：`.artifacts/ui-contract-2026-07-17/pure-white-settings/outer-settings-reference-current.png`、`inner-settings-reference-current.png`。
- 三页即时切换录屏：`.artifacts/ui-contract-2026-07-17/pure-white-settings/navigation-motion.mp4`；逐帧接触表未发现中间淡入/渐变帧。
- OPPO 已连接并通过 `推送 APK → pm install -r` 静默覆盖；外屏实际视口 `1140 × 2616 / 442dpi / font scale 1.0`，现有 App 数据保留。

## Findings history

1. 第一轮外屏运行状态与队列偏高，添加任务首屏露出不足；已压缩卡片内边距、指标间距和队列可视高度。
2. 第一轮历史卡片过高；已对外屏单独缩短时间轴、收紧卡片内边距，并限制标题为单行。
3. 第一轮平台图标因 PNG 白底参与着色而显示为色块；已移除白底并复核真实品牌图形。
4. 第一轮设置存储卡片信息过长；已改为“下载路径 / 导出 cookies.txt”两段结构，保持信息真实且更贴近参考。
5. 内屏三页统一在 `2248 × 2480` 下复核，未沿用预览板中不统一的展示长度。

## 2026-07-17 外屏底部导航白底与强化选中态

- 源视觉真值：`/var/folders/bn/4pttdwp96xj5mv8rbhdlscn40000gn/T/codex-clipboard-2dfa6a48-fc93-4f07-a8ad-cb1c6220826a.png`，结合用户明确要求“底栏纯白，选中态像内屏一样使用完整模块底色”。
- 实现截图：`.artifacts/ui-contract-2026-07-17/bottom-navigation-white/outer-settings.png`。
- 视口：OPPO Find N5 外屏 `1140 × 2616 / 442dpi / font scale 1.0`。
- 状态：设置页已选中，通知权限已授予，无系统弹层遮挡。
- 聚焦并排证据：`.artifacts/ui-contract-2026-07-17/bottom-navigation-white/reference-vs-implementation.png`。
- 全屏证据：实现截图确认页面内容未被底栏遮挡，本次源图只提供了底栏聚焦区域，因此不对页面其他区域做像素级匹配。

### 必查保真面

- 字体与字阶：三个菜单保留既有 `labelMedium`；选中文字只提升到 `SemiBold`，未改变字号、行高或文案。
- 间距与布局：三项等宽，选中承载面为 `64dp` 高的完整圆角模块；触控目标未缩小，手势区无遮挡。
- 颜色与令牌：导航承载面显式为 `#FFFFFF` 且 `tonalElevation = 0dp`；选中模块复用内屏 `SelectedSage`，图标与文字使用深绿主色，与未选中灰色形成明确对比。
- 图标与资产：保留现有 Material 导航图标，未替换为字符、Emoji 或临时绘制资产；截图无模糊、裁切或透明边缘异常。
- 文案与内容：“主页 / 历史 / 设置”未变，导航语义与路由不变。

### Findings history

1. 改动前外屏导航整体带浅绿色调，选中只是图标后的小胶囊；与“纯白承载面 + 明显模块选中态”不符。
2. 已将底栏改为纯白且取消 Material 色调抬升，并把选中底色扩展到整个菜单模块。
3. 后续同视口截图与聚焦并排图未发现可执行的 P0 / P1 / P2 差异；源图中整栏浅绿色是用户明确要求修正的问题，不作为保留项。

final result: passed

## 2026-07-18 失败任务保留与三页顶部减负

- 队列不再因为任务进入 `FAILED` 就移除该行；错误摘要直接显示在原行，同一位置提供“重试”和删除操作。
- 等待、下载、网络等待和失败状态共用固定行高与固定操作槽，状态变化不再推动列表位置跳动。
- 历史页删除顶部“只展示已完成的下载记录”与“平台 / 时间”标题，筛选选项本身保留；设置页删除标题下方说明。
- 任务结束通知不再统一宣称“已完成”；有失败时明确提示失败数量、任务已保留在队列，可查看原因并重试。
- 模拟器验证：历史、设置与数据仓库定向测试 `18/18`；首页整类交互测试 `15/15`；全部 `0 failures`。
- OPPO 外屏失败态证据：`/tmp/nzf-failed-queue.png`；失败行保留原列表位置、错误摘要、重试和删除入口。
- OPPO 外屏完成后证据：`/tmp/nzf-live-after-completion-home.png`；旧失败尝试仍在队列，较新的同作品任务已完成且没有反向删除失败记录。
- 历史页证据：`/tmp/nzf-live-completed-history.png`；顶部说明与“平台 / 时间”标题已删除，筛选选项紧接搜索框，新完成记录出现在首位。
- 设置页证据：`/tmp/nzf-settings-trimmed.png`；标题下方说明已删除，纯白功能卡直接进入首屏。
- YouTube `/live/Z98F3gyNFqM` 真机成品已由 MediaStore 读回：1,628,377,415 B、`video/mp4`。最后一段音频为恢复链路验证而人工补齐，因此该条吞吐显示 `--/s`，不作为速度验收样本。
- 最终回归：102 项 JVM、15 项三页模拟器界面测试、13 项 Python 解析测试全部 0 失败；`lintDebug` 与 `assembleDebug` 通过。

final result: passed

## 2026-07-17 队列删除与标题精简

- 视觉来源：用户提供的外屏首页截图，以及“删除标题区网络提示、删除历史管理入口、每条队列增加删除”的明确批注。
- 同视口并排证据：`.artifacts/ui-contract-2026-07-17/queue-live-fix/reference-vs-oppo.png`。
- 历史页真机证据：`/tmp/nzf-history-no-manage.png`。
- 首页标题只保留 App 图标与名称；运行状态卡仍保留真实网络状态，避免删除必要反馈。
- 历史页标题移除无实际操作的齿轮和“管理”，搜索、筛选和记录三点菜单保持不变。
- 队列每条作品右侧增加独立红色删除图标；分辨率、文件大小、选择框和主下载按钮未被替换或遮挡。
- 删除操作直接作用于对应队列任务；活动任务会先安全停止写入、再删除任务与吞吐记录，等待任务则直接删除。
- 模拟器仓库测试 `8/8`、整类界面测试 `13/13`，均为 `0 failures`；OPPO 真机已显示删除图标并完成两处标题精简。

### Findings history

1. 改动前标题区网络文案重复了运行状态卡信息；已删除重复项，保留唯一状态所有者。
2. 历史“管理”只有装饰、无独立行为；已删除，真实操作继续由每条记录三点菜单承载。
3. 队列删除采用单行小图标，不新增二次编辑模式；同视口比较未发现文字、分辨率或文件大小被裁切。

final result: passed

## 2026-07-17 外屏队列密度与滚动提示

- 视觉来源：用户要求把首页空间优先让给批量视频，移除重复的队列分类行和独占一行的“选择当前页”，并在溢出时明确提示可继续上下滑动。
- OPPO 外屏证据：`.artifacts/ui-contract-2026-07-17/queue-density/outer-home-4-items.png`，真实视口 `1140 × 2616 / 442dpi / font scale 1.0`。
- 同视口前后对比：`.artifacts/ui-contract-2026-07-17/queue-density/reference-vs-oppo.png`。
- “队列 / 等待网络 / 已跳过”三标签已删除；状态继续由每条任务状态点与上方运行状态统一表达。
- 全选操作收进“下载队列”标题行，不再单独占一行；开始下载仍与列表保持同一卡片、无二次编辑确认。
- 外屏队列可视上限由约 2 条扩展到约 4–5 条；8 条队列的模拟器测试确认右侧绿色滚动轨道和当前位置滑块可见。
- 添加任务的平台支持说明已移入输入框占位文字；成功加入提示不再占用卡片底部，错误、超时等必要反馈仍保留。
- 模拟器整类界面测试 `14/14`、0 失败；JVM、lint、Debug APK 构建门禁均通过。

### Findings history

1. 第一轮队列只有约 108dp 可视高度，真实外屏看起来像只有两条作品；已提高到 252dp 上限，同时保持空队列紧凑。
2. 列表只有在内容确实溢出时才显示滚动条，避免 1–4 条任务时出现无意义装饰。
3. 仍保留每条任务的选择、分辨率、文件大小和删除入口；空间压缩没有通过删掉核心操作实现。

final result: passed
